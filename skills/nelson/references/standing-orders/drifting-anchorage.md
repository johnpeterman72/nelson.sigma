# Standing Order Φ₅: Drifting Anchorage

! Task scope expanding beyond the sailing orders without re-scoping.

**Symptoms:** CPTs add features or refactors not in the battle plan · mission metric disconnected from active work · token and time budgets overrun without matching progress · CPT creates new functions, files, or environment variables duplicating existing ones instead of extending them · existing implementation deprecated, bypassed, or shadowed rather than modified.

**Remedy:** drift detected at a quarterdeck checkpoint → re-scope ∨ split the task. Work outside the sailing orders → defer ∨ add explicitly with ADM approval. Parallel implementation instead of extending existing code → direct the CPT to remove the duplicate and modify the existing implementation. Task already completed → partial rollback per `references/damage-control/partial-rollback.md` → re-task with explicit modification targets.
